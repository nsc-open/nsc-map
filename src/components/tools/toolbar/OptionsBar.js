/**
 * OptionsBar is the options setting form of the current active tool
 * There can be only single OptionsBar to be renderered at a time, cause there should be only one active tool at a time
 * 
 * Different tools would have it own options bar defined
 */

